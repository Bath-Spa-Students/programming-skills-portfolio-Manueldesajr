'''Write a function called favorite_book() that accepts one parameter, title.\
   The function should print a message,\
   such as One of my favorite books is Alice in Wonderland.\
   Call the function, making sure to include a book title as an argument in the function call.'''

# Define a function called favourite_book that accepts one parameter, title.
def favourite_book(title):
    # The function prints a message about the favourite book.
    print(f"One of my favourite books is {title}. ")

# Call the function with a book title as an argument .
favourite_book(" Be your motivation")
